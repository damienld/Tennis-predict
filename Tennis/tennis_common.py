import datetime

DateStartOffSeason = (1, 12)
DateEndOffSeason = (25, 12)


def calc_datediff_withoutoffseason(date2: datetime, date1: datetime) -> int:
    if date2 > datetime(olddate.year, 11, 1) and olddate < datetime(
        olddate.year, 12, 25
    ):
        date - datetime(year, 11, 1)
